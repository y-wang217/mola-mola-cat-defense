{
  "name": "siege",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter @siege/web dev",
    "build": "pnpm --filter @siege/web build",
    "test": "pnpm --filter @siege/sim test",
    "lint": "pnpm --filter @siege/web lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
